artifacts_root: artifacts # this is the root directory for all artifacts
#use this config file to change the path of any artifact and any other config related to data ingestion 


data_ingestion:
  root_dir: artifacts/data_ingestion
  source_URL: https://drive.google.com/file/d/1vlhZ5c7abUKF8xXERIw6m9Te8fW7ohw3/view?usp=sharing # source url of the data if the data is changes then change the url
  local_data_file: artifacts/data_ingestion/data.zip
  unzip_dir: artifacts/data_ingestion